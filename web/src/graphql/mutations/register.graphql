mutation Register($options: RegisterDTO!) {
    register(options: $options) {
        errors {
            field
            message
        }
        user {
            ...RegularUser
        }
    }
}
