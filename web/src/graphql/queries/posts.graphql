query Posts($limit: Int!, $cursor: Int, $keyword: String) {
    posts(limit: $limit, cursor: $cursor, keyword: $keyword) {
        posts {
            ...RegularPost
        }
        hasMore
    }
}
